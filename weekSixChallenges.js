// // given an array see if it is a palindromes and return true or false.

// //using an array or string method
const pArray = ["moerm"];
const arrReversed = pArray.reverse();
if (pArray !== arrReversed) {
  console.log(false);
} else {
  console.log(true);
}

//using a loop function
